{
  "name": "@ticketchain/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile && node scripts/copy-abis.mjs",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy-local.ts --network hardhat",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.ts --network mstTestnet",
    "deploy:marketplace:testnet": "hardhat run scripts/deploy-marketplace-testnet.ts --network mstTestnet",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.13.10",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.19",
    "typescript": "^5.8.2"
  }
}
